let memncmp(a: char *, b: char *, n: u32): int => {
  let i: u32 => 0;
  while i < n => {
    let x: char => a[i];
    let y: char => b[i];
    if x != y => return x - y;
    i => i +1;
  }
  return 0;
}